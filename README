Installation via git :
git clone git@github.com:Bastes/vim.config.git ~/.vim && ln ~/.vim/.vimrc ~/.vimrc && cd ~/.vim && git submodule init && git submodule update

Update via git :
cd ~/.vim && git pull origin master && git submodule init && git submodule update
